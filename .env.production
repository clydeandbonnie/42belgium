# Canonical site URL for these landing pages (read by src/lib/seo.ts).
# The LPs are served from the discover subdomain; canonical/hreflang/schema
# must point there, not at the main 42belgium.be marketing site.
NEXT_PUBLIC_SITE_URL=https://discover.42belgium.be
